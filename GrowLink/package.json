{
  "name": "growlink-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs --check-only",
    "verify": "node scripts/build.mjs --check-only",
    "preview": "python3 -m http.server 3000"
  },
  "engines": {
    "node": ">=18"
  }
}
